import React, { FC } from 'react';
import Header from '../Header';
import PageContent from '../PageContent';

const App: FC = () => {
    return (
        <>
            <Header />

            <PageContent />
        </>
    );
};

export default App;
